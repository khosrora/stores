import express, { Router } from 'express';

// import {} from '@products/controllers/orders.controller';

const router: Router = express.Router();

export const ordersRoutes = (): Router => {
  return router;
};
